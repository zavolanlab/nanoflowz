name: nf-plot
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      # This tells pip to install directly from the branches of the GitHub repos
      - git+https://github.com/zavolanlab/zavolab_pyutils.git@dev

# Cache Bust: Added April 27 14:34 to force pip pulling the latest version of zavolab_pyutils